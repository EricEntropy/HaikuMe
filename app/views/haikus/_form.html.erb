<%= form_for haiku do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label :content %>
    <%= f.text_field :content %>
    <%= f.collection_check_boxes :theme_ids, Theme.all, :id, :name %>
    <%= f.submit %>
<% end %>